---
# API key for the external provider
# Replace REPLACE_ME with the actual API key before applying
apiVersion: v1
kind: Secret
metadata:
  name: gpt-oss-120b-api-key
  namespace: external-models
  labels:
    inference.llm-d.ai/ipp-managed: "true"
    inference.networking.k8s.io/bbr-managed: "true"
type: Opaque
stringData:
  api-key: REPLACE_ME
