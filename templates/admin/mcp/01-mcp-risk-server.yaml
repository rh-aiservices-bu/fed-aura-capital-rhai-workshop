---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-risk-server
  namespace: mcp-servers
  labels:
    app: mcp-risk-server
    app.kubernetes.io/name: mcp-risk-server
    app.kubernetes.io/component: mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-risk-server
  template:
    metadata:
      labels:
        app: mcp-risk-server
    spec:
      securityContext: {}
      containers:
        - name: mcp-risk-server
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: false
          image: quay.io/rh-ai-quickstart/mortgage-ai-api:latest
          imagePullPolicy: Always
          command: ["python", "-m", "src.mcp_server"]
          ports:
            - name: http
              containerPort: 8081
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-risk-server
  namespace: mcp-servers
  labels:
    app: mcp-risk-server
    app.kubernetes.io/name: mcp-risk-server
    app.kubernetes.io/component: mcp
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: mcp-risk-server
