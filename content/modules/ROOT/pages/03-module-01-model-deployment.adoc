== Module 1: Model Catalog and Deployment
:navtitle: Model Catalog and Deployment
:imagesdir: ../assets/images
:toc:
:toc-placement: preamble
:icons: font

Model choices and compression - smaller size, same intelligence.

Lenora depends on language models for every conversation, reasoning step, and tool decision.
In the complete platform architecture, those models sit behind the MaaS gateway and provide inference to all five persona-specific agents.
This module zooms in on that model-serving layer.

.Module 1 focus: self-hosted model inference
image::fed-aura-platform-architecture-models-focus.svg[The Fed Aura Capital platform architecture with the self-hosted models layer highlighted,align=center]

Fed Aura Capital wants to replace per-token dependence on external model vendors with private, self-hosted inference.
You will browse the Red Hat OpenShift AI Model Catalog, deploy models on CPU and GPU, scale inference with llm-d, and measure the effect of quantization on latency.
The endpoint produced here becomes the intelligence layer that later modules govern, connect to tools and knowledge, observe, and secure.

Estimated time: *25 minutes*

== Learning Objectives

By the end of this module you will be able to:

* Navigate the RHOAI Model Catalog and identify validated models
* Deploy an LLM on CPU and understand hardware profile selection
* Deploy with GPU and llm-d distributed inference
* Benchmark quantized vs unquantized models and interpret TTFT metrics

== Exercise 1: Explore the Model Catalog

Browse the RHOAI Model Catalog, filter by task type, and identify NVIDIA Nemotron models available for deployment.

TBD

== Exercise 2: Deploy a Model on CPU

Walk through the Deploy wizard, select a CPU hardware profile, and configure a vLLM ServingRuntime for inference.

TBD

=== Quick Test: GenAI Playground

Before moving on, open the GenAI Playground from the RHOAI dashboard and point it at the CPU model you just deployed.
Send a simple prompt - something like _"What is a fixed-rate mortgage?"_ - and confirm that the model responds.
This is the fastest way to verify that the inference endpoint is live, and in Module 3 you will return to the Playground to compare multiple models and shape Lenora's behavior with system prompts.

== Exercise 3: Deploy with GPU and llm-d

Configure a GPU hardware profile, enable "Distributed Inference with llm-d", and set the number of replicas.

TBD

== Exercise 4: Benchmark Quantized vs Unquantized

Run a live benchmark against both model variants and compare results.
Key metrics to observe: TTFT of 35.6ms (quantized) vs 60.4ms (unquantized), a 1.69x speedup, and prefix cache hit rate.

TBD

== Contrast: Cloud API vs Self-hosted on RHOAI

[cols="1,2,2", options="header"]
|===
| Dimension
| Without (Cloud API)
| With (Self-hosted on RHOAI)

| Cost
| Per-token billing
| Fixed infrastructure

| Latency
| Network round-trip
| Local inference 35.6ms TTFT

| Data Sovereignty
| Data leaves network
| Stays in-cluster

| Model Choice
| Vendor's selection only
| Full catalog

| Scaling
| Vendor-managed
| llm-d with controlled replicas
|===

== Module Summary

=== What You Accomplished

* Browsed the RHOAI Model Catalog and located validated Nemotron models
* Deployed an LLM using both CPU and GPU hardware profiles
* Enabled llm-d distributed inference with configurable replicas
* Ran quantized vs unquantized benchmarks and measured TTFT improvements

=== Key Takeaways

* Quantized models deliver comparable quality at significantly lower latency (1.69x speedup in TTFT)
* Self-hosted inference eliminates per-token costs and keeps data inside the cluster
* llm-d provides scalable, distributed inference without manual load-balancing

=== What's Next

In the next module you will publish the deployed model as a Models as a Service (MaaS) endpoint, enabling governed access for every team in the organization.
